# docs/specs/

Specs written by the Context agent for structural changes to this repo
(new plugin, schema change, anything touching > 2 files or altering what
other machines install). One spec file per change, named
`YYYY-MM-DD-short-slug.md`. Single-file edits don't need a spec.
