{
  "name": "dcoe-roster",
  "version": "3.7.0",
  "description": "DCOE shared core (CORE.md) for Claude Code, matching CLAUDE.md v3.5. Agent bodies live at ~/.claude/agents/ (user level) and are deployed automatically by a SessionStart hook running agent-bodies-reference/bootstrap.mjs — no manual per-machine copy step. This plugin still ships no agent definitions of its own, so the loader never double-lists them.",
  "author": {
    "name": "Tebello Lelosa"
  }
}
