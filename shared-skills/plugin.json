{
  "name": "shared-skills",
  "version": "1.0.0",
  "description": "Cross-project Claude Code Skills proven on at least one real project: dev server staleness verification, safe reads of Office files that may be open elsewhere, and 3 UI-specific patterns (reusing existing overlay primitives, sweeping shared UI conventions, verifying UI cardinality against generator output). Installs to user scope so it applies automatically across every project on a machine.",
  "author": {
    "name": "Tebello Lelosa"
  }
}
